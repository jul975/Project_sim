[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
markers =
    dev: fast core safety checks
    validate: broader validation checks
    full: full validation including baselines
    slow: long-running tests
    rng: RNG isolation tests
    regime: regime-level validation tests